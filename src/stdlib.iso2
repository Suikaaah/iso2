type bool = False | True
type 'a option = None | Some of 'a
type ('a, 'e) result = Ok of 'a | Error of 'e

(* [] and :: are also available *)
type 'a list = Nil | Cons of 'a * 'a list

(* literals like 420 are also available *)
type nat = Z | S of nat

